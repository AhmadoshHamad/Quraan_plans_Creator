name: Build and Deploy Vite React App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm install

      - name: Build Vite React App
        run: npm run build

      - name: Verify Build Directory
        run: ls -la dist

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.5
        with:
          host: 46.202.172.230
          username: ${{ secrets.SSH_USERNAME }}
          port: 65002
          password: ${{ secrets.SSH_PASSWORD }}
          source: "dist/*"
          target: "domains/hotpink-magpie-870177.hostingersite.com/public_html/"

      - name: Move files to public_html
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: 46.202.172.230
          username: ${{ secrets.SSH_USERNAME }}
          port: 65002
          password: ${{ secrets.SSH_PASSWORD }}
          script: |
            cd domains/hotpink-magpie-870177.hostingersite.com/public_html/
            mv ~/dist/* .
